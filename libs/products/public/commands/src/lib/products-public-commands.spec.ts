import { productsPublicCommands } from './products-public-commands';

describe('productsPublicCommands', () => {
  it('should work', () => {
    expect(productsPublicCommands()).toEqual('products-public-commands');
  });
});
