import { productsPublicServices } from './products-public-services';

describe('productsPublicServices', () => {
  it('should work', () => {
    expect(productsPublicServices()).toEqual('products-public-services');
  });
});
