import { productsPublicQueries } from './products-public-queries';

describe('productsPublicQueries', () => {
  it('should work', () => {
    expect(productsPublicQueries()).toEqual('products-public-queries');
  });
});
